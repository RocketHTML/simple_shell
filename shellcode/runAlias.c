#include "shell.h"
/**
 * runAlias - checks for aliases and runs them
 * @cmd: pointer to command struct
 * @bg: if should run in background
 * Return: -1 on failure, 0 for success
 */
int runAlias(cmdstruct *cmd, int bg)
{
	return (-1);
}
